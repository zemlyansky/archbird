{
  "schema_version": 2,
  "project": "verify-reference",
  "description": "Reference fixture for typed architecture verification.",
  "layers": [
    {
      "name": "reference",
      "role": "core",
      "language": "python",
      "globs": ["spec.py", "runner.py"],
      "required": true
    }
  ],
  "components": [
    {"name": "core", "paths": ["spec.py"], "required": true},
    {"name": "runner", "paths": ["runner.py"], "required": true}
  ]
}
